import Link from "next/link";
import { getOpportunities } from "@/lib/store";
export default function OpportunitiesPage(){ const opps=getOpportunities(); return <div><h1>Ranked Opportunities</h1><p className="muted">Prioritized by Saga Dog Corp keyword fit, urgency, technical fit, and public value.</p><div className="card"><table className="table"><thead><tr><th>Priority</th><th>Opportunity</th><th>Agency</th><th>Score</th><th></th></tr></thead><tbody>{opps.map(opp=><tr key={opp.noticeId}><td><span className={`badge ${opp.priority?.toLowerCase()}`}>{opp.priority}</span></td><td><strong>{opp.title}</strong><br/><span className="muted">{opp.noticeId}</span></td><td>{opp.agency}</td><td>{opp.score}/100</td><td><Link className="btn secondary" href={`/opportunities/${encodeURIComponent(opp.noticeId)}`}>Open</Link></td></tr>)}</tbody></table></div></div>; }
