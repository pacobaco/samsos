import { getOpportunities, getSubmissions } from "@/lib/store";
export default function MonthlyReportPage(){ const opps=getOpportunities(); const submissions=getSubmissions(); return <div><h1>Monthly Report</h1><section className="grid"><div className="card"><h2>{opps.length}</h2><p className="muted">Opportunities</p></div><div className="card"><h2>{opps.filter(o=>o.priority==="P0").length}</h2><p className="muted">P0</p></div><div className="card"><h2>{submissions.length}</h2><p className="muted">Submissions</p></div></section><section className="card" style={{marginTop:20}}><h2>Top Opportunities</h2><pre className="code">{JSON.stringify(opps.slice(0,5), null, 2)}</pre></section></div>; }
