import { getSubmissions } from "@/lib/store";
export default function SubmissionsPage(){ const submissions=getSubmissions(); return <div><h1>Submission Tracker</h1><p className="muted">Demo state for Vercel. Connect a database for production persistence.</p><div className="card"><table className="table"><thead><tr><th>Status</th><th>Opportunity</th><th>Priority</th><th>Next Action</th></tr></thead><tbody>{submissions.map(s=><tr key={s.noticeId}><td>{s.status}</td><td><strong>{s.title}</strong><br/><span className="muted">{s.noticeId}</span></td><td><span className={`badge ${s.priority?.toLowerCase()}`}>{s.priority}</span></td><td>{s.nextAction}</td></tr>)}</tbody></table></div></div>; }
